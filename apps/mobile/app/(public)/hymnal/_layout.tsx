import { Stack } from 'expo-router';

export default function HymnalLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
